# Portfolio

Standalone premium developer portfolio built with Next.js 16, React 19, TypeScript, and the App Router.

## Start

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.

Read `PROJECT_GUIDE.md` before implementing features. AI agents must also read `AGENTS.md` and the relevant skill under `.agents/skills/`.
# portfolio-web
